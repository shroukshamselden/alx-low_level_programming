#include"main.h"
/**
 *_islower - check if the character is lowercase
 *@c: is the character to be checked
 * Return: (1) if c is lower case else it return (0)
 */
int _islower(int c)
{
return (c >= 'a' && c <= 'z');
}
